---
const links = [
  { label: 'Sobre mí', href: '#sobre-mi' },
  { label: 'Servicios', href: '#servicios' },
  { label: 'Proceso', href: '#proceso' },
  { label: 'Blog', href: '#blog' },
  { label: 'Contacto', href: '#contacto' },
];
---

<header class="nav-root" id="navRoot">
  <div class="nav-island" id="navIsland">
    <!-- Warm ambient glow behind scrolled island -->
    <div class="island-glow" aria-hidden="true"></div>

    <!-- Logo -->
    <a href="#inicio" class="nav-mark" aria-label="Inicio" data-nav-reveal>
      <img src="/logo-white.webp" alt="Lucía" class="mark-logo mark-logo--dark" width="auto" height="32" />
      <img src="/logo-dark.webp" alt="Lucía" class="mark-logo mark-logo--light" width="auto" height="32" />
    </a>

    <!-- Desktop Links -->
    <nav class="nav-links" aria-label="Principal" id="navLinks">
      {links.map((l, i) => (
        <a href={l.href} class="nav-link" data-nav-link data-nav-reveal style={`transition-delay: ${i * 60 + 200}ms`}>
          <span class="link-text">{l.label}</span>
          <span class="link-track" aria-hidden="true">
            <span class="link-fill"></span>
          </span>
        </a>
      ))}
    </nav>

    <!-- CTA -->
    <a href="#contacto" class="nav-cta" data-nav-reveal style="transition-delay: 500ms">
      <span class="cta-text">Reservar</span>
      <svg width="12" height="12" viewBox="0 0 12 12" fill="none" aria-hidden="true">
        <path d="M1 6h10M6 1l5 5-5 5" stroke="currentColor" stroke-width="1.2" stroke-linecap="round" stroke-linejoin="round"/>
      </svg>
    </a>

    <!-- Mobile Trigger -->
    <button class="nav-trigger" id="menuTrigger" aria-label="Abrir menú" aria-expanded="false" data-nav-reveal>
      <span class="trigger-line"></span>
      <span class="trigger-line"></span>
      <span class="trigger-label">Menú</span>
    </button>
  </div>

  <!-- Scroll progress hairline -->
  <div class="scroll-hairline" aria-hidden="true">
    <div class="hairline-fill" id="hairlineFill"></div>
  </div>

    <!-- Mobile Overlay -->
  <div class="nav-overlay" id="menuOverlay" aria-hidden="true">
    <div class="overlay-backdrop" aria-hidden="true"></div>

    <div class="overlay-card">
      <div class="overlay-grain" aria-hidden="true"></div>
      <div class="overlay-card__inner">
        
        <!-- Compact Header -->
        <div class="overlay-top" aria-hidden="true">
          <div class="overlay-deco">
            <svg width="48" height="48" viewBox="0 0 80 80" fill="none">
              <circle cx="40" cy="40" r="38" stroke="rgba(184, 168, 138, 0.2)" stroke-width="1"/>
              <circle cx="40" cy="40" r="28" stroke="rgba(184, 168, 138, 0.15)" stroke-width="1"/>
              <circle cx="40" cy="40" r="18" stroke="rgba(184, 168, 138, 0.1)" stroke-width="1"/>
              <path d="M40 22 L40 58 M22 40 L58 40" stroke="rgba(184, 168, 138, 0.12)" stroke-width="0.5" stroke-linecap="round"/>
            </svg>
          </div>
          <p class="overlay-greeting">Bienvenida</p>
        </div>

        <!-- Navigation -->
        <nav class="overlay-nav" aria-label="Principal">
          {links.map((l, i) => (
            <a href={l.href} class="overlay-link" style={`transition-delay: ${i * 55 + 120}ms`}>
              <span class="link-number">0{i + 1}</span>
              <span class="link-label">{l.label}</span>
              <span class="link-indicator" aria-hidden="true"></span>
            </a>
          ))}
        </nav>

        <!-- Footer -->
        <div class="overlay-footer">
          <div class="footer-divider" aria-hidden="true"></div>
          <div class="footer-block">
            <span class="footer-heading">¿Empezamos?</span>
            <a href="#contacto" class="overlay-cta">
              <span>Pide tu primera sesión</span>
              <svg width="14" height="14" viewBox="0 0 14 14" fill="none">
                <path d="M1 7h12M7 1l6 6-6 6" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
            </a>
          </div>
          <div class="overlay-meta">
            <span>luciamillanpsico@gmail.com</span>
            <span class="meta-sep">·</span>
            <span>722 535 914</span>
          </div>
        </div>
      </div>
    </div>
  </div>
</header>

<script>
  const trigger = document.getElementById('menuTrigger');
  const overlay = document.getElementById('menuOverlay');
  const navLinks = overlay.querySelectorAll('.overlay-link');
  const navRoot = document.getElementById('navRoot');
  const navDesktopLinks = document.querySelectorAll('[data-nav-link]');
  const hairlineFill = document.getElementById('hairlineFill');

  // Menu toggle
  function openMenu() {
    overlay.classList.add('open');
    trigger.classList.add('open');
    trigger.setAttribute('aria-expanded', 'true');
    trigger.setAttribute('aria-label', 'Cerrar menú');
    overlay.setAttribute('aria-hidden', 'false');
    document.body.style.overflow = 'hidden';
    navRoot.classList.add('menu-open');
  }
  function closeMenu() {
    overlay.classList.remove('open');
    trigger.classList.remove('open');
    trigger.setAttribute('aria-expanded', 'false');
    trigger.setAttribute('aria-label', 'Abrir menú');
    overlay.setAttribute('aria-hidden', 'true');
    document.body.style.overflow = '';
    navRoot.classList.remove('menu-open');
  }
  trigger.addEventListener('click', () => {
    overlay.classList.contains('open') ? closeMenu() : openMenu();
  });
  navLinks.forEach(l => l.addEventListener('click', closeMenu));
  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape' && overlay.classList.contains('open')) closeMenu();
  });
  overlay.addEventListener('click', (e) => {
    if (e.target === overlay || e.target.classList.contains('overlay-backdrop')) closeMenu();
  });

  // ── Smart hide / show on scroll direction ──────────────────
  let lastScrollY = window.scrollY;
  let ticking = false;
  const topThreshold = 80;
  const hideThreshold = 140;

  function handleScroll() {
    const currentScrollY = window.scrollY;
    const scrollingDown = currentScrollY > lastScrollY;
    const scrollingUp = currentScrollY < lastScrollY;
    const pastTop = currentScrollY > topThreshold;

    if (!pastTop) {
      navRoot.classList.remove('is-hidden', 'is-visible');
    } else {
      if (scrollingDown && currentScrollY > hideThreshold) {
        navRoot.classList.add('is-hidden');
        navRoot.classList.remove('is-visible');
      }
      if (scrollingUp) {
        navRoot.classList.add('is-visible');
        navRoot.classList.remove('is-hidden');
      }
    }

    // Hairline progress
    if (hairlineFill) {
      const docHeight = document.documentElement.scrollHeight - window.innerHeight;
      const progress = docHeight > 0 ? (currentScrollY / docHeight) * 100 : 0;
      hairlineFill.style.transform = 'scaleX(' + (progress / 100) + ')';
    }

    lastScrollY = currentScrollY;
    ticking = false;
  }

  window.addEventListener('scroll', () => {
    if (!ticking) {
      requestAnimationFrame(handleScroll);
      ticking = true;
    }
  }, { passive: true });
  handleScroll();

  // Active section tracking
  const sections = ['#inicio', '#sobre-mi', '#servicios', '#proceso', '#blog', '#contacto'];
  const sectionElements = sections.map(s => document.querySelector(s)).filter(Boolean);

  function updateActiveLink() {
    const scrollPos = window.scrollY + 200;
    let activeSection = '#inicio';
    sectionElements.forEach(section => {
      if (section.offsetTop <= scrollPos) {
        activeSection = '#' + section.id;
      }
    });
    navDesktopLinks.forEach(link => {
      const href = link.getAttribute('href');
      link.classList.toggle('active', href === activeSection);
    });
  }
  window.addEventListener('scroll', updateActiveLink, { passive: true });
  updateActiveLink();

  // Entrance reveal
  const revealEls = document.querySelectorAll('[data-nav-reveal]');
  revealEls.forEach((el, i) => {
    el.style.opacity = '0';
    el.style.transform = 'translateY(12px)';
    const delay = parseFloat(el.style.transitionDelay) || (i * 60);
    setTimeout(() => {
      el.style.transition = 'opacity 0.9s var(--ease-out-expo), transform 0.9s var(--ease-out-expo), color 0.6s ease';
      el.style.opacity = '1';
      el.style.transform = 'translateY(0)';
    }, 200 + delay);
  });
</script>

<style>
  :global(:root) {
    --site-header-height: calc(clamp(14px, 2vw, 22px) * 2 + 64px);
  }

  /* ═══════════════════════════════════════════════════════════
     NAVIGATION — Chameleon Island
     Transparent over hero, warm glass over content.
     Color-inverts seamlessly as user scrolls.
     ═══════════════════════════════════════════════════════════ */

  .nav-root {
    --nav-cream: #f5f0e8;
    --nav-ink: #2f2d28;
    --nav-ink-soft: #49463f;
    --nav-sage: #6f7d67;
    --nav-sage-deep: #56645a;
    --nav-gold: #b8a88a;
    --nav-gold-soft: rgba(184, 168, 138, 0.14);
    --nav-line: rgba(111, 125, 103, 0.14);
    --nav-line-soft: rgba(111, 125, 103, 0.08);

    position: fixed;
    top: 0; left: 0; right: 0;
    z-index: 900;
    display: flex;
    justify-content: center;
    padding: clamp(14px, 2vw, 22px) 3vw;
    pointer-events: none;
    transition: transform 0.6s var(--ease-out-expo);
  }

  .nav-root.is-hidden {
    transform: translateY(calc(-100% - 40px));
  }

  .nav-root.is-visible {
    transform: translateY(0);
  }

  /* Prevent island from hiding when menu is open */
  .nav-root.menu-open {
    transform: translateY(0) !important;
  }

  /* ── Island ───────────────────────────────────────────────── */
  .nav-island {
    pointer-events: auto;
    position: relative;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 2rem;
    width: min(1280px, 94vw);
    min-height: 64px;
    padding: 0.55rem 2.2rem;
    border-radius: 22px;
    transition:
      background 0.7s ease,
      backdrop-filter 0.7s ease,
      box-shadow 0.7s ease,
      border-color 0.7s ease,
      min-height 0.55s var(--ease-out-expo),
      padding 0.55s var(--ease-out-expo),
      gap 0.55s var(--ease-out-expo),
      width 0.6s var(--ease-out-expo),
      border-radius 0.6s var(--ease-out-expo);
  }

  /* At top: transparent, light text for dark hero */
  .nav-root:not(.is-visible):not(.menu-open) .nav-island {
    background: transparent;
    border: 1px solid transparent;
    box-shadow: none;
    backdrop-filter: none;
    -webkit-backdrop-filter: none;
  }

  /* Scrolled: warm cream glass with ink text */
  .nav-root.is-visible .nav-island,
  .nav-root.menu-open .nav-island {
    z-index: 902;
    width: min(720px, 90vw);
    min-height: 0;
    padding: 0.5rem 0.55rem 0.5rem 0.9rem;
    gap: 1.1rem;
    border-radius: 100px;
    background: rgba(251, 248, 241, 0.82);
    backdrop-filter: blur(14px) saturate(1.1);
    -webkit-backdrop-filter: blur(14px) saturate(1.1);
    border: 1px solid var(--nav-line);
    box-shadow:
      0 8px 32px rgba(47, 45, 40, 0.06),
      0 2px 8px rgba(47, 45, 40, 0.04),
      inset 0 1px 0 rgba(255, 255, 255, 0.6);
  }

  /* Keep island above overlay when menu is open */
  .nav-root.menu-open .nav-island {
    z-index: 904;
  }

  /* Warm ambient glow behind scrolled island */
  .island-glow {
    position: absolute;
    inset: -60% -30%;
    border-radius: 50%;
    background: radial-gradient(ellipse at 50% 50%, rgba(184, 168, 138, 0.1) 0%, transparent 60%);
    pointer-events: none;
    opacity: 0;
    transform: scale(0.85);
    transition: opacity 0.9s ease, transform 0.9s ease;
    z-index: -1;
  }
  .nav-root.is-visible .island-glow,
  .nav-root.menu-open .island-glow {
    opacity: 1;
    transform: scale(1);
  }

  /* ── Logo ─────────────────────────────────────────────────── */
  .nav-mark {
    display: grid;
    align-items: center;
    text-decoration: none;
    transition: opacity 0.35s ease;
    flex-shrink: 0;
    position: relative;
    z-index: 1;
  }
  .nav-mark:hover { opacity: 0.72; }

  .mark-logo {
    height: 32px;
    width: auto;
    grid-row: 1;
    grid-column: 1;
    transition: opacity 0.5s ease, transform 0.5s var(--ease-out-expo);
  }
  .nav-mark:hover .mark-logo {
    transform: scale(1.05);
  }

  /* Dark logo — visible at top (over dark hero) */
  .mark-logo--dark {
    opacity: 1;
  }
  .nav-root.is-visible .mark-logo--dark,
  .nav-root.menu-open .mark-logo--dark {
    opacity: 0;
  }

  /* Light logo — visible when scrolled (over cream island) */
  .mark-logo--light {
    opacity: 0;
  }
  .nav-root.is-visible .mark-logo--light,
  .nav-root.menu-open .mark-logo--light {
    opacity: 1;
  }

  /* ── Desktop Links ────────────────────────────────────────── */
  .nav-links {
    display: flex;
    align-items: center;
    gap: 2.2rem;
    transition: gap 0.55s var(--ease-out-expo);
  }
  .nav-root.is-visible .nav-links {
    gap: 1.2rem;
  }

  .nav-link {
    position: relative;
    font-family: var(--font-body);
    font-size: var(--type-meta);
    font-weight: 500;
    letter-spacing: 0.24em;
    text-transform: uppercase;
    color: rgba(245, 240, 232, 0.7);
    padding: 6px 0;
    text-decoration: none;
    transition: color 0.5s ease;
    text-shadow: 0 1px 8px rgba(0, 0, 0, 0.18);
  }
  .nav-link:hover,
  .nav-link.active {
    color: rgba(245, 240, 232, 0.95);
  }
  /* Scrolled: ink colors */
  .nav-root.is-visible .nav-link {
    color: rgba(47, 45, 40, 0.6);
    text-shadow: none;
  }
  .nav-root.is-visible .nav-link:hover,
  .nav-root.is-visible .nav-link.active {
    color: var(--nav-ink);
  }

  /* Elegant underline track */
  .link-track {
    position: absolute;
    bottom: 0; left: 0;
    width: 100%;
    height: 1px;
    background: rgba(196, 170, 148, 0.12);
    overflow: hidden;
    opacity: 0;
    transition: opacity 0.4s ease, background 0.4s ease;
  }
  .nav-root.is-visible .link-track {
    background: rgba(111, 125, 103, 0.12);
  }
  .nav-link:hover .link-track,
  .nav-link.active .link-track {
    opacity: 1;
  }
  .link-fill {
    display: block;
    width: 100%; height: 100%;
    background: rgba(196, 170, 148, 0.55);
    transform: translateX(-101%);
    transition: transform 0.55s var(--ease-out-expo), background 0.4s ease;
  }
  .nav-root.is-visible .link-fill {
    background: var(--nav-sage);
  }
  .nav-link:hover .link-fill,
  .nav-link.active .link-fill {
    transform: translateX(0);
  }

  /* ── CTA ──────────────────────────────────────────────────── */
  .nav-cta {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 9px 20px;
    background: rgba(245, 240, 232, 0.08);
    border: 1px solid rgba(196, 170, 148, 0.2);
    border-radius: 999px;
    font-family: var(--font-body);
    font-size: var(--type-meta);
    font-weight: 500;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: rgba(245, 240, 232, 0.92);
    text-decoration: none;
    transition: all 0.5s var(--ease-out-expo);
    flex-shrink: 0;
    text-shadow: 0 1px 6px rgba(0, 0, 0, 0.12);
    position: relative;
    z-index: 1;
    overflow: hidden;
  }
  .nav-cta::before {
    content: '';
    position: absolute;
    inset: 0;
    background: rgba(245, 240, 232, 0.06);
    transform: scaleX(0);
    transform-origin: right;
    transition: transform 0.5s var(--ease-out-expo);
  }
  .nav-cta:hover::before {
    transform: scaleX(1);
    transform-origin: left;
  }
  .nav-cta:hover {
    background: rgba(245, 240, 232, 0.14);
    border-color: rgba(196, 170, 148, 0.35);
    color: #fff;
    transform: translateY(-1px);
    box-shadow: 0 6px 24px rgba(0, 0, 0, 0.14);
  }
  /* Scrolled: sage gradient CTA */
  .nav-root.is-visible .nav-cta {
    background: linear-gradient(180deg, rgba(111, 125, 103, 0.92), rgba(86, 100, 90, 0.92));
    border-color: rgba(111, 125, 103, 0.3);
    color: #fbf8f1;
    text-shadow: none;
    box-shadow: 0 4px 16px rgba(86, 100, 90, 0.12);
  }
  .nav-root.is-visible .nav-cta::before {
    background: rgba(255, 255, 255, 0.08);
  }
  .nav-root.is-visible .nav-cta:hover {
    background: linear-gradient(180deg, rgba(111, 125, 103, 1), rgba(86, 100, 90, 1));
    border-color: rgba(111, 125, 103, 0.45);
    transform: translateY(-2px);
    box-shadow: 0 8px 24px rgba(86, 100, 90, 0.18);
  }
  .nav-cta svg {
    position: relative;
    z-index: 1;
    transition: transform 0.4s var(--ease-out-expo);
    color: rgba(196, 170, 148, 0.5);
  }
  .nav-cta:hover svg {
    transform: translateX(2px);
    color: rgba(196, 170, 148, 0.8);
  }
  .nav-root.is-visible .nav-cta svg {
    color: rgba(251, 248, 241, 0.7);
  }

  /* ── Scroll hairline ──────────────────────────────────────── */
  .scroll-hairline {
    position: absolute;
    top: 0; left: 0;
    width: 100%; height: 1px;
    background: rgba(196, 170, 148, 0.04);
    overflow: hidden;
    pointer-events: none;
    opacity: 0;
    transition: opacity 0.6s ease;
  }
  .nav-root.is-visible .scroll-hairline {
    opacity: 1;
  }
  .hairline-fill {
    height: 100%;
    width: 100%;
    transform: scaleX(0);
    transform-origin: left;
    background: linear-gradient(90deg, var(--nav-sage), rgba(111, 125, 103, 0.2));
    will-change: transform;
    transition: transform 0.05s linear;
  }

  /* ── Mobile Trigger ───────────────────────────────────────── */
  .nav-trigger {
    display: none;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 5px;
    cursor: pointer;
    z-index: 902;
    background: none;
    border: none;
    flex-shrink: 0;
    position: relative;
    padding: 8px;
    -webkit-tap-highlight-color: transparent;
    touch-action: manipulation;
    transition: transform 0.3s var(--ease-out-expo);
  }
  .nav-trigger:active {
    transform: scale(0.92);
  }
  .trigger-line {
    display: block;
    width: 20px;
    height: 1.2px;
    background: rgba(245, 240, 232, 0.7);
    transition: transform 0.4s var(--ease-out-expo), opacity 0.3s ease, background 0.5s ease, width 0.3s var(--ease-out-expo);
    border-radius: 1px;
  }
  .trigger-line:last-child {
    width: 14px;
    margin-left: auto;
    margin-right: auto;
  }
  .nav-trigger:hover .trigger-line:last-child {
    width: 20px;
  }
  .nav-root.is-visible .trigger-line {
    background: rgba(47, 45, 40, 0.5);
  }
  .nav-trigger.open .trigger-line:first-child {
    transform: translateY(3.1px) rotate(45deg);
    width: 20px;
    background: rgba(245, 240, 232, 0.85);
  }
  .nav-trigger.open .trigger-line:last-child {
    transform: translateY(-3.1px) rotate(-45deg);
    width: 20px;
    background: rgba(245, 240, 232, 0.85);
  }
  .nav-root.menu-open .trigger-line,
  .nav-root.menu-open .nav-trigger.open .trigger-line {
    background: rgba(47, 45, 40, 0.65);
  }
  .trigger-label {
    display: none;
    font-family: var(--font-body);
    font-size: 0.6rem;
    font-weight: 500;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: rgba(245, 240, 232, 0.5);
    margin-top: 2px;
    transition: color 0.5s ease, opacity 0.3s ease;
  }
  .nav-root.is-visible .trigger-label {
    color: rgba(47, 45, 40, 0.35);
  }
  .nav-trigger.open .trigger-label {
    opacity: 0;
  }

  /* ── Mobile Overlay — Editorial Warm Panel ──────────────── */
  .nav-overlay {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    width: 100vw;
    height: 100vh;
    height: 100dvh;
    z-index: 903;
    display: flex;
    align-items: flex-end;
    justify-content: center;
    opacity: 0;
    visibility: hidden;
    transition: opacity 0.5s var(--ease-out-expo), visibility 0s 0.5s;
    pointer-events: none;
    touch-action: manipulation;
  }

  .nav-overlay.open {
    opacity: 1;
    visibility: visible;
    transition: opacity 0.5s var(--ease-out-expo), visibility 0s 0s;
    pointer-events: auto;
  }

  /* Soft diffused backdrop */
  .overlay-backdrop {
    position: absolute;
    inset: 0;
    background: rgba(22, 18, 15, 0.35);
    backdrop-filter: blur(8px) saturate(0.9);
    -webkit-backdrop-filter: blur(8px) saturate(0.9);
    opacity: 0;
    transition: opacity 0.5s ease;
    -webkit-transform: translateZ(0);
    transform: translateZ(0);
  }
  .nav-overlay.open .overlay-backdrop {
    opacity: 1;
  }

  /* The panel — warm bottom sheet */
  .overlay-card {
    position: relative;
    z-index: 2;
    width: 100%;
    max-height: 70vh;
    max-height: 70dvh;
    overflow-y: auto;
    overscroll-behavior: contain;
    -webkit-overflow-scrolling: touch;
    background: linear-gradient(180deg, #faf8f3 0%, #f3efe6 100%);
    border-radius: 24px 24px 0 0;
    box-shadow:
      0 -16px 56px rgba(47, 45, 40, 0.08),
      0 -4px 12px rgba(47, 45, 40, 0.04);
    transform: translateY(40px);
    opacity: 0;
    transition: transform 0.55s var(--ease-out-expo), opacity 0.5s var(--ease-out-expo);
    padding: 0 0 env(safe-area-inset-bottom, 0px) 0;
    -webkit-transform: translateY(40px);
    will-change: transform, opacity;
  }
  .nav-overlay.open .overlay-card {
    transform: translateY(0);
    opacity: 1;
    -webkit-transform: translateY(0);
  }

  .overlay-card__inner {
    padding: 1.5rem clamp(1.5rem, 6vw, 3rem) 1.75rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    position: relative;
    z-index: 2;
  }

  /* Subtle paper grain texture overlay */
  .overlay-grain {
    position: absolute;
    inset: 0;
    border-radius: 32px 32px 0 0;
    opacity: 0.03;
    pointer-events: none;
    z-index: 1;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.85' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)'/%3E%3C/svg%3E");
    background-repeat: repeat;
    background-size: 128px 128px;
  }

  /* Drag handle */
  .overlay-card::before {
    content: '';
    display: block;
    width: 36px;
    height: 3px;
    background: rgba(47, 45, 40, 0.1);
    border-radius: 2px;
    margin: 10px auto 0;
    position: relative;
    z-index: 2;
    transition: background 0.3s ease;
  }
  .overlay-card:active::before {
    background: rgba(47, 45, 40, 0.18);
  }

  /* ── Top Decorative Block ──────────────────────────────── */
  .overlay-top {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
    margin-bottom: 1rem;
    opacity: 0;
    transform: translateY(-6px);
    transition: opacity 0.5s var(--ease-out-expo) 0.1s, transform 0.5s var(--ease-out-expo) 0.1s;
  }
  .nav-overlay.open .overlay-top {
    opacity: 1;
    transform: translateY(0);
  }

  .overlay-deco {
    width: 48px;
    height: 48px;
    display: flex;
    align-items: center;
    justify-content: center;
  }
  .overlay-deco svg {
    width: 100%;
    height: 100%;
    animation: slowRotate 20s linear infinite;
  }
  @keyframes slowRotate {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
  }

  .overlay-greeting {
    font-family: var(--font-display);
    font-style: italic;
    font-weight: 400;
    font-size: 0.75rem;
    color: rgba(47, 45, 40, 0.35);
    letter-spacing: 0.04em;
  }

  /* ── Overlay Nav Links — Editorial Style ───────────────── */
  .overlay-nav {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.15rem;
    width: 100%;
    margin: 0 0 1rem;
    padding: 0;
  }

  .overlay-link {
    display: inline-flex;
    align-items: baseline;
    justify-content: center;
    gap: 0.5rem;
    padding: 0.6rem 0.75rem;
    -webkit-tap-highlight-color: transparent;
    touch-action: manipulation;
    opacity: 0;
    transform: translateY(8px);
    transition: opacity 0.45s var(--ease-out-expo), transform 0.45s var(--ease-out-expo);
    text-decoration: none;
    border-bottom: none;
    position: relative;
  }
  .nav-overlay.open .overlay-link {
    opacity: 1;
    transform: translateY(0);
  }

  .link-number {
    font-family: var(--font-body);
    font-size: 0.55rem;
    font-weight: 500;
    letter-spacing: 0.1em;
    color: rgba(184, 168, 138, 0.7);
    vertical-align: super;
    transition: color 0.35s ease;
  }
  .overlay-link:hover .link-number {
    color: rgba(111, 125, 103, 0.9);
  }

  .link-label {
    font-family: var(--font-display);
    font-style: italic;
    font-weight: 400;
    font-size: clamp(1.6rem, 5vw, 2rem);
    color: #2f2d28;
    line-height: 1.2;
    letter-spacing: 0.01em;
    transition: color 0.35s ease;
  }
  .overlay-link:hover .link-label,
  .overlay-link:active .link-label {
    color: #6f7d67;
  }

  /* Elegant underline indicator on hover */
  .link-indicator {
    position: absolute;
    bottom: 0.35rem;
    left: 50%;
    transform: translateX(-50%) scaleX(0);
    width: 18px;
    height: 1px;
    background: #b8a88a;
    opacity: 0;
    transition: opacity 0.35s var(--ease-out-expo), transform 0.35s var(--ease-out-expo);
  }
  .overlay-link:hover .link-indicator,
  .overlay-link:active .link-indicator {
    opacity: 1;
    transform: translateX(-50%) scaleX(1);
  }

  /* ── Overlay Footer — Editorial ────────────────────────── */
  .overlay-footer {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.75rem;
    width: 100%;
    margin-top: 0;
    padding-top: 0.75rem;
    opacity: 0;
    transform: translateY(6px);
    transition: opacity 0.5s var(--ease-out-expo), transform 0.5s var(--ease-out-expo);
    transition-delay: 0s;
  }
  .nav-overlay.open .overlay-footer {
    opacity: 1;
    transform: translateY(0);
    transition-delay: 0.25s;
  }

  .footer-divider {
    width: 28px;
    height: 1px;
    background: rgba(184, 168, 138, 0.35);
    margin-bottom: 0.25rem;
  }

  .footer-block {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
  }

  .footer-heading {
    font-family: var(--font-body);
    font-size: 0.55rem;
    font-weight: 500;
    letter-spacing: 0.22em;
    text-transform: uppercase;
    color: rgba(47, 45, 40, 0.25);
  }

  .overlay-cta {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.6rem;
    width: auto;
    padding: 12px 28px;
    background: linear-gradient(180deg, #6f7d67, #56645a);
    border: none;
    border-radius: 100px;
    font-family: var(--font-body);
    font-size: 0.7rem;
    font-weight: 500;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    text-align: center;
    color: #fbf8f1;
    text-decoration: none;
    box-shadow: 0 4px 16px rgba(86, 100, 90, 0.18);
    transition: transform 0.35s var(--ease-out-expo), box-shadow 0.35s var(--ease-out-expo);
    -webkit-tap-highlight-color: transparent;
    touch-action: manipulation;
  }
  .overlay-cta:active {
    transform: scale(0.97);
    box-shadow: 0 2px 8px rgba(86, 100, 90, 0.2);
  }
  .overlay-cta svg {
    transition: transform 0.35s var(--ease-out-expo);
    flex-shrink: 0;
  }
  .overlay-cta:active svg {
    transform: translateX(2px);
  }

  .overlay-meta {
    display: flex;
    align-items: center;
    justify-content: center;
    flex-wrap: wrap;
    gap: 0.8rem;
    font-family: var(--font-body);
    font-size: 0.65rem;
    font-weight: 400;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    text-align: center;
    color: rgba(47, 45, 40, 0.4);
  }
  .meta-sep {
    opacity: 0.3;
  }

  /* ── Responsive ───────────────────────────────────────────── */
  @media (max-width: 1024px) {
    :global(:root) {
      --site-header-height: calc(12px * 2 + 58px);
    }

    .nav-links { display: none; }
    .nav-cta { display: none; }
    .nav-trigger { display: flex; }
    .trigger-label { display: block; }
    .nav-island {
      width: min(1280px, 96vw);
      min-height: 58px;
      padding: 0.5rem 1.6rem;
      gap: 1.4rem;
      border-radius: 18px;
    }
    .nav-root.is-visible .nav-island {
      width: clamp(280px, 55vw, 540px);
      min-height: 0;
      padding: 0.45rem 0.6rem;
      gap: 1rem;
    }
    .mark-logo { height: 28px; }
  }

  @media (max-width: 768px) {
    :global(:root) {
      --site-header-height: calc(12px * 2 + 52px);
    }

    .nav-root { padding: 12px 3vw; }
    .nav-island {
      width: min(1280px, 96vw);
      min-height: 52px;
      padding: 0.45rem 1.3rem;
      gap: 1.1rem;
      border-radius: 16px;
    }
    .nav-root.is-visible .nav-island {
      width: clamp(180px, 50vw, 360px);
      min-height: 0;
      padding: 0.4rem 0.6rem;
    }
    .mark-logo { height: 26px; }
    .overlay-card__inner {
      padding: 1.25rem clamp(1.25rem, 5vw, 2rem) 1.5rem;
    }
    .link-label { font-size: clamp(1.5rem, 4vw, 1.75rem); }
    .overlay-footer { gap: 0.75rem; padding-top: 0.75rem; }
    .overlay-cta {
      width: auto;
      padding: 12px 24px;
      letter-spacing: 0.18em;
    }
    .overlay-meta {
      row-gap: 0.3rem;
      column-gap: 0.5rem;
      letter-spacing: 0.12em;
    }
  }

  @media (max-width: 380px), (max-height: 720px) {
    .overlay-card__inner {
      padding: 1.25rem clamp(1rem, 4vw, 1.5rem) 1.5rem;
    }
    .overlay-top { margin-bottom: 0.75rem; }
    .overlay-deco { width: 40px; height: 40px; }
    .overlay-greeting { font-size: 0.7rem; }
    .overlay-nav { gap: 0.1rem; }
    .overlay-link { padding: 0.5rem 0.4rem; }
    .link-label { font-size: clamp(1.35rem, 5vw, 1.6rem); }
    .link-number { font-size: 0.45rem; }
    .overlay-footer { gap: 0.6rem; padding-top: 0.5rem; }
    .overlay-cta { padding: 11px 22px; }
    .trigger-label { display: none; }
  }

  @media (prefers-reduced-transparency: reduce) {
    .nav-root.is-visible .nav-island,
    .nav-root.menu-open .nav-island {
      background: rgba(251, 248, 241, 0.96);
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .nav-island,
    .nav-link,
    .nav-cta,
    .mark-logo,
    .trigger-line,
    .island-glow,
    .overlay-link,
    .overlay-card,
    .overlay-backdrop,
    .overlay-footer,
    .overlay-top,
    .link-indicator {
      transition: none !important;
    }
    .overlay-deco svg {
      animation: none !important;
    }
  }
</style>
